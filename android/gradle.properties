# 🔧 Increase memory limit for Gradle (important for large Flutter builds)
org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=8G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError

# ✅ Enable AndroidX & Jetifier (required for Flutter 3+)
android.useAndroidX=true
android.enableJetifier=true

# ⚡ Enable parallel and daemon builds (makes Gradle faster)
org.gradle.parallel=true
org.gradle.daemon=true

# 🚀 Enable build caching to reuse results and save time
org.gradle.caching=true

# 🧩 Only configure modules when needed (reduces memory load)
org.gradle.configureondemand=true

# 🐍 Enable incremental Kotlin compilation
kotlin.incremental=true

# 💪 Suppress obsolete Java 8 warnings (optional)
#android.suppressUnsupportedJava8Warning=true
